<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="CellStyle">
        <item name="android:background">@drawable/btn_toggle</item>
        <item name="android:layout_margin">2dp</item>
        <item name="android:textOff">Off</item>
        <item name="android:textOn">On</item>
        <item name="android:minHeight">5dp</item>
        <item name="android:minWidth">5dp</item>
        <item name="android:textSize">@dimen/cell_text_size</item>
        <item name="android:height">@dimen/cell_size</item>
        <item name="android:width">@dimen/cell_size</item>
    </style>
    <style name="RoundBtn">
        <item name="android:layout_margin">2dp</item>
        <item name="android:textSize">20sp</item>
        <item name="android:layout_height">@dimen/cell_size</item>
        <item name="android:layout_width">@dimen/cell_size</item>
        <item name="android:height">@dimen/cell_size</item>
        <item name="android:width">@dimen/cell_size</item>
    </style>

</resources>
